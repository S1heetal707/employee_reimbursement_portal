<h1>New Employee for <%= @company.name %></h1>
<%= form_with model: [@company, @employee] do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.submit 'Create Employee' %>
  </div>
<% end %>
